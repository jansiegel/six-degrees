# Umami Cloud website UUID. When set AND NODE_ENV=production,
# the Umami tracking script is loaded and page views + the
# search_submitted custom event are sent to cloud.umami.is.
# Leave unset locally - tracker stays off in dev and tests.
NEXT_PUBLIC_UMAMI_WEBSITE_ID=
